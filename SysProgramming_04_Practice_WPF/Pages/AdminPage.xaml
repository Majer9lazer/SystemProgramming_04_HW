<Page x:Class="SysProgramming_04_Practice_WPF.Pages.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SysProgramming_04_Practice_WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="AdminPage">

    <Grid>
        <ListView x:Name="OnlineUsersListView">
            <ListView.View>
                <GridView>
                  <GridView.Columns>
                        <GridViewColumn Header="UserId" DisplayMemberBinding="{Binding Path=UserId}"></GridViewColumn>
                        <GridViewColumn Header="UserMail" DisplayMemberBinding="{Binding Path=UserMail}"></GridViewColumn>
                        <GridViewColumn Header="UserLogin" DisplayMemberBinding="{Binding Path=UserLogin}"></GridViewColumn>
                        <GridViewColumn Header="UserPassword" DisplayMemberBinding="{Binding Path=UserPassword}"></GridViewColumn>
                        <GridViewColumn Header="IsOnline" DisplayMemberBinding="{Binding Path=IsOnline}"></GridViewColumn>
                  </GridView.Columns> 
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
